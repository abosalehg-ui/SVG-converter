-r requirements.txt

pytest>=9.1.1
pytest-cov>=5.0

# Installed so CI exercises BOTH block-averaging paths: tests/test_parity.py
# asserts the NumPy fast path and the pure-Python fallback agree byte for byte.
numpy>=1.24
